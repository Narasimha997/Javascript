let a=9;
let b=23;
console.log(a<b);
/* we also have smaller than (<) and smaller than or equal
to operators (<=). Let's have a look at the smaller than operator, as it is very similar to
the previous ones. */
a=9;
b=9;
console.log(a<=b);
// Smaller than or equal to(<=)