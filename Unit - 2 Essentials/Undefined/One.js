// Undefined Data Type
let a;
console.log(a);                 // It is the first case here, we didn't define any value to the a variable we get undefined in output
// Manually assigning undefined to the variable 
let b =undefined
console.log(b);