// Double Equla(==)
const a=5;
const b="5";
console.log(a==b);        // This Double Equal (==) only checks the value it won't check the data type
// If the values are same I, mean above example a=5 which mean number but b="5" string
// When we use double equal we get true in the output because double equal only checks value it won't check data type
