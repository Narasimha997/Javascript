// Strict Equal (===)
let a=5;
let b="5"
console.log(a===b);
// In the Above case we have done same thing in the perivous example.
// In that exapmple we get true.
// Now, we get flase because Strict Equal(===) means it checks the value and data type should be same then we get true otherwise we get false.
// So, that's why we get false in the output not only the values same but also the data type should be same.
// Example - 2
let c=5;
let d=5;
console.log(c===d);
// Here the values and the data types are same simple output is true.
// Examplem - 3
let e="4"
let f="5"
console.log(e===f);
// Here, the output is false why because the data types are same but the values are not same so we get false.
// Example - 4
let g="7"
let h="7"
console.log(g===h);
// Now, the output is true so the data type and the values are same so we get true.