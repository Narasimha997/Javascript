let strtoNr="hello"
strtoNr=Number(strtoNr)
console.log(strtoNr,typeof strtoNr);
// Obsreve the output and read again the code here,the strig converted into number the output will be NaN(Not a Number)
//  Why because "hello" is string so the Number() conversion it doesn't convert into number