// Post Decrement
let a=5;
console.log(a--);            // It is similar to the Addition Method after subtracting the number it show sonly old number 
console.log(a);                  // Then whenever we want see the update then at time we get the updated number in the console
// Pre decrement
let b=5;
console.log(--b);          // After using pre debrement operator we get the 4 in the output directly
console.log(b);