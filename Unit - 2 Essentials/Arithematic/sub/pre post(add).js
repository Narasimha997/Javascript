// Post Increment
let a=8;
console.log(a++);        // Here it add a number to the given number but in output we only get old number 2 only
console.log(a);            // But here after addition of a number the output is 3
// Pre Increment
let b=8;
console.log(++b);             // Now the output is 9 so it add a number and also update it for the output 
console.log(b);               